---
import "@fontsource-variable/nunito";

import BaseLayout from "../../../layouts/BaseLayout.astro";
import Logo from "./_logo-3.png";
import Modes from "./_modes.png";
import { ArrowDown } from "lucide-svelte";
---

<BaseLayout title="Super Streamer Lobby 3">
  <div
    class="font-semibold scroll-smooth h-screen overflow-y-auto overflow-x-hidden"
  >
    <div class="flex flex-col ssl-bg w-screen px-2 items-center">
      <header class="relative h-screen grid place-content-center max-w-3xl">
        <div class="flex flex-col items-center">
          <img
            src={Logo.src}
            alt="Super Streamer Lobby 3"
            style="filter: drop-shadow(-14px 14px 0px hsla(0, 0%, 0%, 25%));"
          />
          <div>
            <p class="px-7 text-xl text-center max-md:text-lg">
              The community event featuring over 200 streamers and their viewers
              battling it out in one TETR.IO match.
            </p>
          </div>
          <div
            class="bg-white rounded shadow-xl px-4 py-2 text-xl w-fit mt-5 flex flex-col items-center whitespace-nowrap max-sm:text-base"
          >
            <p class="font-bold ssl-hyper-text">
              <span>X Days</span>,
              <span>X hours</span>,
              <span>X Minutes</span>,
              <span>X Seconds</span>
            </p>
            <p class="text-zinc-600 text-sm max-sm:text-xs">
              6th of April, 6PM
            </p>
          </div>
        </div>
        <a
          class="absolute bottom-8 left-1/2 -translate-x-1/2 bg-white shadow rounded-full p-1"
          href="#about"
        >
          <ArrowDown size="2rem" />
        </a>
      </header>
      <!-- Main event info -->
      <section class="contents">
        <!-- Agenda Card -->
        <div id="about" class="max-w-3xl w-full flex gap-2 max-md:flex-wrap">
          <div class="flex flex-col">
            <h2 class="font-extrabold text-xl">Main Event</h2>
            <div class="bg-white rounded p-2 max-w-md shadow">
              <p>
                The main event consists of 8 standard games and 8 gimmick games,
                with each game lasting 3-8 minutes. Gimmick games are a staple
                of Super Streamer Lobbies, in which we run several custom
                gamemodes to maximize laughs and clippable moments.
              </p>
            </div>
          </div>
        </div>
        <!-- Agenda bullets Card -->
        <!-- <div class="max-w-3xl w-full flex gap-2 max-md:flex-wrap">
          <div class="flex flex-col">
            <h2 class="font-extrabold text-5xl">16 Games</h2>
            <ul>
              <li>8 Standard Games</li>
              <li>8 Custom Modes</li>
            </ul>
          </div>
        </div> -->
        <!-- Game modes -->
        <div
          class="ssl-dynamic-scale relative w-full py-20 my-7"
          style={{
            "--width": `${Modes.width}px`,
            "--height": `${Modes.height}px`,
            height: "calc(var(--height)/var(--scale))",
          }}
        >
          <div
            class="ssl-scrolling-banner absolute h-full w-[calc(120vw)] top-0 -left-10 -rotate-6"
            style={{
              backgroundImage: `url('${Modes.src}')`,
              backgroundSize:
                "calc(var(--width)/var(--scale)) calc(var(--height)/var(--scale))",
              backgroundRepeat: "repeat-x",
              backgroundPositionY: "center",
              filter: "drop-shadow(-7px 7px 4px hsla(0, 0%, 0%, 25%))",
            }}
          >
          </div>
        </div>
        <!-- Info Card -->
        <div class="max-w-3xl w-full flex items-end flex-col">
          <h2 class="font-extrabold text-xl">What is a super lobby?</h2>
          <div class="bg-white rounded p-2 max-w-md shadow">
            <p>
              A Super Lobby is a TETR.IO lobby with at least 100 players, with
              no upper limit to the player count. Everyone competes in a Battle
              Royale format until just 1 player remains, with typically around
              300 to 400 players participating in each event. It might just be
              one of the largest Battle Royale formats ever. Anyone and everyone
              is welcome to play!
            </p>
          </div>
        </div>
      </section>
      <!-- Secondary events -->
      <section class="contents">
        <div class="grid grid-cols-2 py-4 gap-2 max-w-4xl">
          <div class="bg-white rounded p-4 shadow">
            <h2 class="font-extrabold text-xl">
              Warm up games <span class="text-zinc-500">5:30</span>
            </h2>
            <p>
              Half an hour before the event, we hold some rank capped warm up
              games (not reward/badge eligible)
            </p>
          </div>
          <div class="bg-white rounded p-4 shadow">
            <h2 class="font-extrabold text-xl">
              Afterparty showcase <span class="text-zinc-500">8:00</span>
            </h2>
            <p>
              After the games, we host an afterparty showcase on Twitch where
              streamers can submit their clips and highlights to be featured
              along with some interviews and giveaways.
            </p>
          </div>
        </div>
      </section>
      <!-- Spacer -->
      <div class="h-96"></div>
    </div>
    <!-- <div
          class="grid gap-2 text-center grid-cols-2 max-lg:grid-cols-1 min-h-screen"
        >
          <article>
            <h2 class="font-extrabold">About</h2>
            <p>
              The Super Streamer Lobby is a recurring TETR.IO community event
              that features over 200 streamers and their viewers battling it out
              in one giant game for a chance to win twitch subscriptions, YT
              memberships, rewards, and our streamer packages. With the help of
              the TETR.IO admins, we take the Super Lobby format to the extreme
              by breaking the game with some of the weirdest presets and bizarre
              settings. Anyone and everyone is welcome to play!
            </p>
          </article>
          <article>
            <h2 class="font-extrabold">Wait, what's a "Super Lobby"?</h2>
            <p>
              A super lobby is a TETR.IO lobby with over 100 players, with no
              upper limit to the player count. Everyone is versing each other,
              until just 1 player remains. We usually pull 300 - 400 players
              each event. It's kind of like Tetris 99, but on steroids. It might
              just be the largest battle royale format ever.
            </p>
          </article>
        </div> -->
  </div>
</BaseLayout>

<style>
  body {
    font-family: "Nunito Variable", sans-serif;
  }

  .ssl-bg {
    /* background: hsl(339, 70%, 80%);
    background: linear-gradient(
      155deg,
      hsl(339, 70%, 80%) 20%,
      hsl(37, 70%, 80%) 50%,
      hsl(262, 70%, 80%) 80%
    ); */
    background: hsl(262, 100%, 73%);
    background: linear-gradient(
      155deg,
      hsl(339, 100%, 75%) 0%,
      hsl(38, 100%, 75%) 50%,
      hsl(262, 100%, 73%) 100%
    );
  }

  .ssl-hyper-text {
    color: transparent;
    background: linear-gradient(
      90deg,
      hsl(339, 100%, 60%) 0%,
      hsl(38, 100%, 50%) 50%,
      hsl(262, 100%, 60%) 100%
    );
    background-repeat: repeat-x;
    background-clip: text;
    filter: drop-shadow(0 0 4px hsla(0, 0%, 0%, 5%));
  }

  .ssl-dynamic-scale {
    --scale: 3;
  }

  @media not all and (min-width: 920px) {
    .ssl-dynamic-scale {
      --scale: 4;
    }
  }

  @keyframes background-scroll {
    0% {
      background-position-x: 0px;
    }
    100% {
      background-position-x: var(--width);
    }
  }

  .ssl-scrolling-banner {
    animation: background-scroll 42s linear infinite;
  }

  @media not all and (min-width: 768px) {
    .ssl-scrolling-banner {
      animation: background-scroll 56s linear infinite;
    }
  }
</style>
