// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource postgresql {
  url               = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
  provider          = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id                  String    @id @default(cuid())
  name                String    @default("")
  username            String    @unique @default("")
  password            String
  isAdmin             Boolean   @default(false)
  createdAt           DateTime? @default(now())
  from_Event_managers Event[]   @relation("Event_managers")
}

model Event {
  id       String     @id @default(cuid())
  label    String     @default("")
  date     DateTime   @default(now())
  managers User[]     @relation("Event_managers")
  docs     EventDoc[] @relation("EventDoc_event")
}

model EventDoc {
  id       String  @id @default(cuid())
  label    String  @default("")
  document Json    @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
  event    Event?  @relation("EventDoc_event", fields: [eventId], references: [id])
  eventId  String? @map("event")

  @@index([eventId])
}
